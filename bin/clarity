#!/usr/bin/env python3
"""Clarity language CLI entry point."""

import sys
import os

# Add parent directory to path so clarity package is importable
sys.path.insert(0, os.path.dirname(os.path.dirname(os.path.abspath(__file__))))

from clarity.cli import main

main()
